# Simple Interest Calculator

This project is a **Bash script** that calculates simple interest based on user input.

## How it works

1. The script asks the user to input:
   - Principal amount (P)
   - Rate of interest (R)
   - Time period (T)
2. It calculates the simple interest using the formula:
3.  The result is displayed on the terminal.

## Usage

To run the script:

```bash
bash simple-interest.sh
